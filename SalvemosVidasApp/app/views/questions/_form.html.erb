<%= form_with(model: question, local: true) do |form| %>
  <% if question.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(question.errors.count, "error") %> prohibited this question from being saved:</h2>

      <ul>
      <% question.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div id="divPreguntas" class="row col-sm-offset-1">
    <div class="field">
      <%= form.text_field :description, placeholder:"Ingrese su pregunta", class:"color-titulo input-question col-sm-8" %>
    </div>

    <div class="field">
      <%= form.text_field :correct, placeholder:"La opción correcta aquí", class:"input-answer col-sm-offset-1 col-sm-7" %>
    </div>

    <div class="field">
      <%= form.text_field :incorrect1, placeholder:"Opción incorrecta", class:"input-answer col-sm-offset-1 col-sm-7" %>
    </div>

    <div class="field">
      <%= form.text_field :incorrect2, placeholder:"Opción incorrecta", class:"input-answer col-sm-offset-1 col-sm-7" %>
    </div>

    <div class="field">
      <%= form.text_field :incorrect3, placeholder:"Opción incorrecta", class:"input-answer col-sm-offset-1 col-sm-7" %>
    </div>
  </div>

  <div class="actions text-right">
    <%= form.submit "Añadir",name: "boton", class:"btn"%>
    <%= link_to "Salir","/practices/category/all",class:"btn"%>
  </div>
<% end %>
