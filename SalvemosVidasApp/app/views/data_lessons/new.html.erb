<% if signed_in? %>
	<h2 align="center">Creación de Lección</h2>

	<%= render 'form', lesson: @lesson %>

	<div class="col-sm-offset-2 col-sm-10">
        <%= button_to 'Volver', lessons_path, class:"btn" , method: :get %>
    </div>

<% else %>
    <div class="alert alert-danger panel-ancho">
      <h4>Error!</h4>
      <p>Necesita iniciar sesión para acceder a esta dirección.</p>
    </div>
<% end %>